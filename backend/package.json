{
  "name": "Classivo-backend",
  "version": "6.7.0",
  "private": true,
  "scripts": {
    "dev": "go run src/main.go",
    "build": "go build -o bin/main src/main.go",
    "start": "./bin/main",
    "install": "go mod tidy",
    "clean": "rm -rf bin/",
    "test": "go test ./...",
    "fmt": "go fmt ./...",
    "vet": "go vet ./..."
  },
  "engines": {
    "go": ">=1.23.0"
  }
}
